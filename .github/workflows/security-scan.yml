name: Security Scan (Trivy)

on:
  push:
    branches: [develop]
  schedule:
    # Mondays at 06:00 UTC
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  trivy-scan:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - name: provibackend
            context: ./provibackend
          - name: provifrontend
            context: ./ProViFrontend
          - name: redis
            context: ./provibackend/redis
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sudo sh -s -- -b /usr/local/bin v0.70.0
          trivy --version

      - name: Build image
        run: docker build -t ${{ matrix.target.name }}:scan ${{ matrix.target.context }}

      - name: DEBUG - inspect image directly (temporary)
        if: matrix.target.name == 'provibackend'
        run: |
          echo "--- docker images (looking for provibackend:scan) ---"
          docker images
          echo "--- docker inspect provibackend:scan (created/id) ---"
          docker inspect --format '{{.Id}} created={{.Created}}' provibackend:scan
          echo "--- pip show inside the freshly built container ---"
          docker run --rm provibackend:scan pip show msgpack setuptools
          echo "--- dist-info files found inside the freshly built container ---"
          docker run --rm provibackend:scan sh -c "find / -iname 'msgpack*.dist-info' -o -iname 'setuptools*.dist-info' 2>/dev/null"

      - name: Trivy scan (table, for humans)
        run: |
          trivy image \
            --image-src docker \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --ignorefile .trivyignore \
            --exit-code 0 \
            ${{ matrix.target.name }}:scan

      - name: Trivy scan (SARIF for Security tab)
        run: |
          trivy image \
            --image-src docker \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --ignorefile .trivyignore \
            --format sarif \
            --output trivy-${{ matrix.target.name }}.sarif \
            --exit-code 0 \
            ${{ matrix.target.name }}:scan

      - name: Upload SARIF to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-${{ matrix.target.name }}.sarif
          category: trivy-${{ matrix.target.name }}

      - name: Fail on HIGH/CRITICAL
        run: |
          trivy image \
            --image-src docker \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --ignorefile .trivyignore \
            --exit-code 1 \
            ${{ matrix.target.name }}:scan
