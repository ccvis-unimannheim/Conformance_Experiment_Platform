# ProViBackend

FastAPI backend for the ProCon (conformance checking) experiment platform. Handles participant flow, admin experiment configuration, task/idiom generation, and data storage (MongoDB + Redis).

See `Contribute.md` for local setup and known issues.
